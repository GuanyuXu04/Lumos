[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lumos"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["pyyaml"]

[tool.setuptools.packages.find]
where = ["."]
include = ["lumos*"]
